# AI Worklog

## Какие AI-инструменты использовал(а)

Начал с Codex GPT, затем продолжил с Claude Code (Sonnet 5).

## Общее количество времени на разработку приложения

Около 12 часов

## Общее количество израсходованных токенов

Breakdown       Sonnet 5
    
Input           3.5k 
    
Output          975.6k
    
Cache read      759.5M
    
Cache write     7.7M

## Этапы рабочего процесса

**Domain**: я написал базовые классы `Entity`, `ValueObject`.
Я написал сущности и value object'ы, их аттрибуты и логику.
Написал абстрактный репозиторий для порталов.

**Application**: AI создал тонкий сервис для работы с `Portal`. Я сделал DTO и переписал сервис под эти DTO.

**Presentation**: роутеры и DTO.

**Infra**: я сделал с помощью AI две реализации репозиториев — memory и postgresql (SQLAlchemy).

**Tests**: параллельно со всеми этапами разработки бэкенда просил AI писать тесты, проверял и прогонял их, а также просил AI найти слабые места и ошибки.

**Доработка логики**: под конец больше углубился в саму логику портала и его аттрибутов, а также исключения (архитектура позволяет менять их легко). Просил нейронку сделать магические методы для базового `ValueObject`. Додумывал, как должен работать риск, и давал AI команды, например: `перепиши Risk так чтобы при достижении максимального значения Stability, а также при expired_at < 30 секунд, risk.value был максимальным`

**Frontend** — полностью AI. Я только указываю, что изменить.

**Отладка и финальная проверка** — сначала я, потом AI.

## Ключевые промпты по каждому этапу

**Domain**:
> проверь domain область. Найди логические ошибки, предложи улучшения

> value object Risk должен не только высчитывать уровень риска числом, но и возвращать указания на конкретные критические места — высокая нестабильность, низкий уровень энергии, близость закрытия. И пусть get_best_action теперь использует factors из risk

**Application**:
> Напиши тонкий сервис для всех методов у сущности Portal. Только вызывай метод и сохраняй в репозиторий

**Infra**:
> разбей репозитории по пакетам memory и postgres. Каждый репозиторий в отдельном модуле, у каждого пакета свой base.py с абстракцией репозитория

**Tests**:
> покрой всю область domain тестами на pytest

> теперь сделай тесты для application

> перепиши тесты domain под обновленный код

**Frontend**:
> работай только внутри frontend/ напиши фронтенд, который будет работать с бэкендом из backend/presentation/api. Должна быть таблица порталов (get_portals_list), при переходе внутрь портала получается более подробная информация (get_portal) и можно выполнять действия в соответствии с реализованными эндпоинтами

**Доработка логики**:
> для ValueObject (backend/src/domain/common/value_object.py) сделай магические методы для всех сравнений, сложения, вычитания. Во всех этих методах проверяй что isinstance(other, type(self)) и сравнивай/складывай/вычитай по self.value

> я изменил твои методы на правильные. Перепиши тесты под эту реализацию

## Какие 5 решений я принял сам

1. Архитектура DDD, слои: Domain, Application, Infrastructure, Presentation.
2. Стэк: PostgreSQL (и Memory для тестов), FastAPI, SQLAlchemy, Alembic, Pydantic, React и т.д.
3. Все исключения придумывал и писал сам.
4. Аттрибуты портала, их основная логика, value object'ы.
5. Организация вычислений риска: нужен отдельный VO `Risk`, который считает уровень риска и факторы риска. Каждый фактор имеет своё значение и вес. Факторы сортируются по формуле (вес × значение). Первый фактор считается самым важным, по нему и считает метод `Portal.get_best_action()`. Клиент получает этот список факторов в той же сортировке по приоритету. AI только придумал формулу вычисления `Risk.value`.
6. Подтягивать данные по порталам с бэка поллингом раз в 1 минуту, при условии, что данные не подтягивались за последние 10 секунд.

## Где AI ошибся или предложил плохое решение

1. При создании имплементаций репозиториев AI положил все репозитории в один модуль без разделения на пакеты memory и postgres. А также не задал `BaseRepository` для каждого.
2. В `application/portal/service.py` AI сделал реализацию обновлений через строку, т.е. можно было указать любой несуществующий метод, и было неудобно для понимания, какие есть методы. Я создал enum для возможных методов, что избавляло от ошибки. И на всякий случай сделал отдельное исключение `InvalidPortalMethodError`.
3. Когда я попросил сделать магические методы для `ValueObject`, AI начал реализовывать эти методы отдельно для каждого VO — `Energy`, `Risk`, `ExpiresAt`, `Stability`. Я поправил его, чтобы он сделал эти методы для базового класса `ValueObject`, так как они подходят под все реализации, при необходимости можно будет переопределить их в будущем.

## Что ты переписал или доработал вручную

Переписал сервис: чтобы он возвращал репозиторий; написал обращения в logs_service; переделал аргумент в методе `PortalService.__change()` из `str` в enum + реализовал `Portal.do_action(method: PortalMethodEnum)`.

В Presentation написал `presentation/api/dto/common` с респонсами через Generic для общего вида всех ответов сервера: `{ok: True, payload: {}, error: {type: "", detail: ""}}`.

`change_logs` полностью писал сам.

## Как я проверял, что приложение действительно работает

Я читал все тесты, которые написал AI, и прогонял их.
Развернул приложение локально и прожал все кнопки, сверил логику с логикой в коде.
Прошёлся указанным в ТЗ обязательным состояниям.

## Что бы я доработал и улучшил, если бы это был реальный продукт

- Разделил бы frontend и backend на два разных репозитория.
- Добавил бы кэширование на методы получения логов с добавлением новых логов и в БД, и в кэш.
